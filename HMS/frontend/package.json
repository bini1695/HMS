{
  "private": true,
  "scripts": {
    "dev": "npm --prefix my-react run dev",
    "build": "npm --prefix my-react run build",
    "lint": "npm --prefix my-react run lint",
    "preview": "npm --prefix my-react run preview"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^18.0.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.24.4",
    "react-router-dom": "^7.18.4"
  }
}
